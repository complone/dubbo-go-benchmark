# dubbo client yaml configure file

dubbo:
  registries:
    demoZK:
      protocol: zookeeper
      address: node1:2181
  consumer:
    references:
      FibProvider:
        protocol: dubbo
        interface: org.apache.dubbo.FibProvider
  logger:
    zap-config:
      level: info
