# dubbo client yaml configure file


dubbo:
  registries:
    demoZK:
      protocol: zookeeper
      address: node2:2181
  consumer:
    references:
      UserProvider:
        protocol: dubbo
        interface: org.apache.dubbo.sample1.UserProvider
  logger:
    zap-config:
      level: info
