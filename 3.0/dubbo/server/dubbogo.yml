# dubbo server yaml configure file

dubbo:
  registries:
    demoZK:
      protocol: zookeeper
      address: node2:2181
  protocols:
    dubbo:
      name: dubbo
      port: 20001
  provider:
#    registry-ids:
#      - zk
    services:
      UserProvider:
        protocol: dubbo
        interface: org.apache.dubbo.sample1.UserProvider
#  logger:
#    zap-config:
#      level: error